public class PartialOrder {
    int[] prefs;
    PartialOrder(int[] prefs){
        this.prefs = prefs;
    }

    //boolean greater(int a, int b){
       // return prefs.indexOf(a) < prefs.indexOf(b);
    //}
}
